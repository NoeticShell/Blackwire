      - name: Commit and push if changed
        run: |
          git config user.name "textwire-bot"
          git config user.email "textwire-bot@users.noreply.github.com"

          # Make sure we're up to date (avoids "fetch first" rejection)
          git fetch origin main
          git pull --rebase origin main

          git add index.html
          git diff --staged --quiet && exit 0

          git commit -m "Update index"

          # Push (retry once in case another run lands first)
          git push origin main || (sleep 3 && git pull --rebase origin main && git push origin main)
name: Build Text Wire

on:
  schedule:
    - cron: "*/30 * * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Install deps
        run: pip install feedparser

      - name: Build
        run: python build.py

      - name: Commit and push if changed
        run: |
          git config user.name "textwire-bot"
          git config user.email "textwire-bot@users.noreply.github.com"
          git add index.html
          git diff --staged --quiet || (git commit -m "Update index" && git push)
